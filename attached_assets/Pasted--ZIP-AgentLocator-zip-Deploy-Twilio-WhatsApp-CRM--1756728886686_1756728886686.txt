עברתי על ה-ZIP (AgentLocator.zip) ובדקתי את כל השכבות: Deploy, Twilio (שיחות), WhatsApp, ו-CRM. הנה הממצאים התמציתיים + תיקונים מדויקים איפה שצריך.

✅ סטטוס כללי (מה יש בפרויקט)
	•	WSGI / Deploy
	•	wsgi.py מנקה אגרסיבית כל EVENTLET_HUB ומגדיר EVENTLET_NO_GREENDNS=1 ✔️
	•	Procfile: python3 -m gunicorn wsgi:app -k eventlet -w 1 -b 0.0.0.0:$PORT ✔️
	•	runtime.txt = python-3.11 ✔️
	•	.replit מכיל ב־[deployment] unset ל-EVENTLET_HUB ✔️ (אבל ראו “לתקן עכשיו” על ה-[env] למטה)
	•	Twilio (שיחות)
	•	קיים server/routes_twilio.py עם endpoints:
/webhook/incoming_call, /webhook/stream_status, /webhook/handle_recording, /webhook/call_status, /webhook/incoming_call_preview, /webhook/test* ✔️
	•	שימוש ב-@require_twilio_signature על נקודות רגישות (נספרו 3 שימושים) ✔️
	•	מעקב מצב stream: server/stream_state.py ✔️
	•	Media Streams WS: server/media_ws_ai.py (ניהול תורות, VAD, barge-in וכו’) ✔️
	•	WhatsApp
	•	ממשק מאוחד: server/api_whatsapp_unified.py עם
/api/whatsapp/send, /webhook/whatsapp/twilio, /webhook/whatsapp/baileys, /messages, /conversations ✔️
	•	שכבת ספקים: server/whatsapp_provider.py (Baileys + Twilio) + תור קבצים ל-Baileys ✔️
	•	CRM / Admin / UI
	•	מודלים: server/models_sql.py, server/db.py ✔️
	•	נתיבי Admin/Business/UI: server/routes_admin.py, server/routes_business_management.py, server/ui/routes.py ✔️
(ב-UI ראיתי /api/crm/contacts ועוד אבני בניין לדשבורדים והרשאות)
	•	Data API: server/data_api.py ✔️
	•	בריאות המערכת
	•	server/health_endpoints.py: /healthz, /readyz, /version ✔️

⸻

⚠️ מה לשפר מיד (קטן אך קריטי)
	1.	ניקוי כפילויות וסותרות ב־requirements.txt
כרגע יש שם שורות כפולות/סותרות:

eventlet==0.36.1        # מופיע פעמיים
greenlet>=3.0,<3.3      # וגם greenlet>=2.0
gunicorn                # וגם gunicorn==21.2.0

תיקון מומלץ (יציב לפרודקשן):

flask==3.0.3
gunicorn==21.2.0
eventlet==0.36.1
greenlet==3.0.3
simple-websocket==1.0.0
flask-cors
flask-sqlalchemy
flask-login
flask-session
flask-mail
flask-limiter
flask-bcrypt
psycopg2-binary
requests
openai
twilio
qrcode
gtts
pillow
pydub
numpy
python-dotenv
schedule
bcrypt
email-validator

הסר כפילויות. נעל גרסאות לשלישייה: eventlet 0.36.1 / greenlet 3.0.3 / gunicorn 21.2.0.

	2.	לנקות את ה-EVENTLET_HUB מה-[env] של .replit
בבלוק [env] עדיין יש:

EVENTLET_HUB = "select"

אמנם ב-[deployment] אתה עושה unset, וגם wsgi.py מנקה, אבל כדי למנוע בלבול בהרצת Workspace – מחק לגמרי את השורה הזאת מה-[env].

	3.	אימות חתימת Twilio בפרודקשן – ודא Secret
יש דקורטור @require_twilio_signature ✔️ אבל זה תלוי ב-TWILIO_AUTH_TOKEN. ודא שב-Secrets של ה-Deploy מוגדרים:
	•	TWILIO_ACCOUNT_SID
	•	TWILIO_AUTH_TOKEN
	•	(ל-WA) TWILIO_WHATSAPP_NUMBER אם משתמשים ב-Twilio ל-WA
	4.	מסלולי Health אחידים
יש /healthz, /readyz, /version. אם יש לך מוניטור חיצוני שמחפש /health – הוסף alias קטן:

@app.route("/health")
def _health_alias():
    return "ok", 200



⸻

🧪 בדיקות מומלצות עכשיו (זריזות)
	1.	בריאות
גלוש ל:
	•	/healthz → 200
	•	/readyz → JSON עם "status": "ready"
	•	/version → JSON עם גרסה/זמן
	2.	Twilio – smoke ללא חתימה (רק לוודא שאין 500)

curl -X POST https://<הדומיין>/webhook/incoming_call -d "CallSid=TEST"
curl -X POST https://<הדומיין>/webhook/stream_status -d "event=media"
curl -X POST https://<הדומיין>/webhook/handle_recording -d "RecordingSid=RE_TEST&CallSid=CA_TEST"

ציפייה: 2xx/4xx מבוקר (לא 500). בפרודקשן אמיתי, בקשות בלי חתימה יקבלו 403 – זה טוב.

	3.	שיחת אמת
	•	אין 31920 WebSocket Handshake Error
	•	אין 15000 Request to /webhook/stream_status not made
	•	לחיצה על * לא גורמת 11205 timeout ל־/webhook/handle_recording.
	4.	WhatsApp
	•	/webhook/whatsapp/twilio (עם חתימה אמיתית) מחזיר 200.
	•	/api/whatsapp/send שולח הודעה (ספק Twilio/Baileys לפי קונפיג).
	•	אם Baileys: ודא שה־bridge קורא את קובץ ה-queue ומוציא הודעות.
	5.	CRM
	•	/api/admin/overview, /api/admin/tenants, /api/crm/contacts מפיקים תוצאות (עם הרשאות מתאימות).
	•	בדוק RBAC (admin / business manager / representative) לפי ה-decorators.

⸻

✅ סיכום “עובר/נכשל”
	•	Deploy + Eventlet/Gunicorn: עובר (אחרי מחיקת EVENTLET_HUB מה-[env] וניקוי requirements).
	•	Twilio שיחות + חתימה: עובר (לוודא Secrets ו-200/403 בהתאם).
	•	WhatsApp מאוחד: עובר (לוודא בחירה בין Twilio/Baileys וריצה של ה-bridge).
	•	CRM + RBAC + מודלים: עובר (בדיקות CRUD והרשאות).
	•	Health & Monitoring: עובר (מומלץ alias ל־/health).

רוצה שאכין לך Pull Request diff מוכן ל:
	1.	ניקוי requirements.txt מהכפילויות,
	2.	הסרת EVENTLET_HUB מה-.replit (בלוק [env]),
	3.	הוספת /health alias,
כדי שתוכל להדביק לאג’נט ולהחיל בשניות?