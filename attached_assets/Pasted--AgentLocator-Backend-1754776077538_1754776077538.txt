סבבה — פתחתי את הזיפ האחרון (הפרויקט תחת תיקייה AgentLocator/) ועשיתי בדיקת מבנה זריזה:
	•	ה-Backend רץ מ־main.py + app.py (Flask).
	•	ה-Frontend הוא React + Vite בתיקיית השורש src/ (קיים src/pages/LoginPage.jsx).
	•	ה־Preview אצלך נתקע כי npm run dev מריץ python main.py & node baileys_client.js — בלי Vite dev ובלי build לפרונט.
	•	TwiML (ב־routes.py) משתמש כרגע ב־maxLength="10" ו־timeout="4" — מומלץ לשנות ל־30/5.

כמו שביקשת: נשאיר רק דף התחברות נקי ב-React ונמנע מלגעת ב-APIים/וובהוקים של שיחות/וואטסאפ/CRM. הנה הוראה אחת-לאחת לסוכן שלך:

⸻

🚦 “Login-Only UI” — להשאיר רק דף התחברות (React/Vite) בלי לפגוע בשרת

כללים:
	•	לא משנים או מוחקים קוד שרת של Twilio/WhatsApp/CRM (קבצי ‎.py נשארים).
	•	ב-Frontend כן מוחקים תבניות/מסכים לא רלוונטיים — נשאר רק Login.
	•	כל שינוי על Branch חדש + PR עם diff.

1) ענף עבודה

git checkout -b ui/login-only

2) Frontend — משאירים רק Login

נמצא אצלך: src/pages/LoginPage.jsx, src/App.jsx, src/main.jsx.
	1.	מחק/הסר את כל שאר הדפים והרואטרים ב-client (רק צד פרונט):

git rm -r src/pages/AdminDashboard.jsx src/pages/CRMPage.jsx src/pages/CallsPage.jsx \
           src/pages/CustomerPage.jsx src/pages/Dashboard.jsx src/pages/WhatsAppPage.jsx 2>/dev/null || true

	2.	החלף את src/App.jsx כך שמציג רק את מסך ההתחברות (בלי Router/Routes/Sidebar וכו’):

// src/App.jsx
import React from 'react';
import LoginPage from './pages/LoginPage';

export default function App() {
  return <LoginPage />;
}

	3.	פשט את src/main.jsx (אין Router, רק mount נקי):

import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode><App /></React.StrictMode>
);

אם LoginPage.jsx משתמש בפונקציית login ל-API, ודא שהקריאות הולכות ל־/api/auth/login ושהן נשלחות עם credentials: 'include'.

	4.	ודא שב־vite.config.js יש proxy ל־API:

server: {
  host: '0.0.0.0',
  port: 3000,
  proxy: {
    '/api': { target: 'http://localhost:5000', changeOrigin: true, secure: false },
    '/webhook': { target: 'http://localhost:5000', changeOrigin: true, secure: false }, // לא חובה ל-Login, אך טוב לדב
    '/socket.io': { target: 'http://localhost:5000', ws: true }
  }
}

3) Backend — לא נוגעים ב-API/וובהוקים, כן מתקנים TwiML פרמטרים

ב־routes.py (זה שמייצר TwiML) עדכן את בלוק <Record> לדרישות:

<Record
  maxLength="30"
  timeout="5"
  playBeep="true"
  action="/webhook/handle_recording"
  method="POST"
  transcribe="false"
  trim="do-not-trim"
  finishOnKey="*"
/>

לא לשנות שמות ראוטים קיימים (למשל /voice/incoming, /webhook/handle_recording) — רק הפרמטרים.

אם אין /health — הוסף בדיקת בריאות קטנה (לא פוגעת בכלום):

# ב-app.py (אחרי יצירת app)
from flask import jsonify
@app.route("/health")
def health(): return jsonify(ok=True), 200

4) Scripts כדי שה-Preview יעבוד

בקובץ package.json בשורש יש כרגע:

"scripts": { "dev": "python main.py & node baileys_client.js" }

הוסף/עדכן כך:

"scripts": {
  "dev": "concurrently -k \"python main.py\" \"vite\"",
  "build": "vite build",
  "preview": "vite preview --host 0.0.0.0 --port 3000",
  "start": "python main.py"
},
"devDependencies": {
  "concurrently": "^9.2.0"
}

מי שרוצה להריץ רק Flask (ללא Vite dev) יכול להשאיר dev כ-python main.py, אבל אז צריך לבנות את הפרונט ל-dist/ ולשרת סטטי (ראו סעיף 5B).

5) הרצה — שתי דרכים תקינות

A) דב נוח (Vite + Flask במקביל)

טרמינל 1:

python3 -m venv .venv && source .venv/bin/activate
python -m pip install -U pip wheel
pip install -r requirements.txt 2>/dev/null || pip install -r server/requirements.txt
python main.py

טרמינל 2:

npm ci
npm run dev   # יריץ Vite על 3000 ו-Flask על 5000 במקביל אם הוגדר עם "concurrently"

בדיקות עשן:

curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5000/health
curl -s -X POST http://localhost:5000/voice/incoming | head

B) Preview/פריסה מינימלית (Flask משרת את React ה-build)

פעם אחת:

npm ci
npm run build   # ייצור dist/

ב־app.py אתחל Flask עם סטטי ל-dist:

from flask import Flask, send_from_directory
app = Flask(__name__, static_folder="dist", static_url_path="/")

@app.route("/")
def serve_index():
    return send_from_directory(app.static_folder, "index.html")

הרצה:

python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt 2>/dev/null || pip install -r server/requirements.txt
python main.py  # ה-Login הסטטי מוגש על פורט 5000

6) מה מוחקים ומה לא
	•	כן למחוק (פרונט בלבד): כל דפי React שאינם src/pages/LoginPage.jsx, וכל נווט/תפריט שמוביל אליהם.
	•	לא מוחקים (שרת): קבצי ‎.py של שיחות/WhatsApp/CRM, מודלים, ראוטים, קבצי בדיקות — נשארים.
	•	Flask templates (Jinja): אפשר להשאיר כרגע (לא נטען אליהם), או להעבירם לתיקייה ארכיון פנימית; רק לא לשבור ראוטים.

7) Acceptance לפני מיזוג
	•	פתיחת http://localhost:3000 (דב) או http://localhost:5000 (פרוד/Preview) מראה רק מסך התחברות.
	•	POST /api/auth/login עובד (200) ומחזיר session cookie.
	•	/health → 200.
	•	ה-webhooks/שיחות ממשיכים לקבל 200.
	•	TwiML כולל <Record maxLength="30" timeout="5" finishOnKey="*">.

⸻

אם תרצה — אכין לך PR-diff מוכן שמבצע בדיוק את:
	•	החלפת App.jsx + פישוט main.jsx;
	•	מחיקות דפי React אחרים;
	•	עדכון TwiML ל-30/5/*;
	•	הוספת scripts ל-package.json;
	•	(אופציונלי) אתחול Flask לשרת את dist/ במקרה build.