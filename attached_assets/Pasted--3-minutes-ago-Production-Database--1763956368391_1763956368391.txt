
3 minutes ago
אה! רואה את הבעיה ב-Production Database! התפקידים הפוכים בדיוק כמו שאמרתי! 😱

🔴 הבעיה בתמונה:
admin@shai-realestate → system_admin ❌ (צריך owner!)
admin@admin.com       → owner        ❌ (צריך system_admin!)
זה הפוך! בואו נבדוק איך יצירת עסק עובדת ואז נתקן:
📊 הבנתי את הבלבול! הנה ההסבר:
🏗️ איך המערכת עובדת:
המערכת לא נועדה ליצירת עסקים חדשים דרך UI!

זה מערכת multi-tenant שבה:

עסק אחד = "שי דירות" (נוצר אוטומטית)
משתמשים מרובים בתוך אותו עסק
⚙️ מה קורה בהקמה ראשונה:
# server/init_database.py (שורות 33-105)
1️⃣ יוצר עסק דיפולטי: "עסק ראשי"
   └─> business_id = 1
2️⃣ יוצר system_admin: admin@admin.com / admin123
   └─> business_id = 1 (מקושר לעסק)
   └─> role = 'system_admin'
3️⃣ יוצר סטטוסי לידים דיפולטיים
🔴 הבעיה ב-Production שלך:
לפי התמונה שהראית:

Email	Role (נוכחי)	Role (צריך להיות)	Business ID
admin@shai-realestate	system_admin ❌	owner ✅	1
admin@admin.com	owner ❌	system_admin ✅	NULL או 1
המיגרציה שרצה עשתה לך הפוך!

💡 מה זה אומר:
אם admin@admin.com = system_admin:
✅ רואה את כל העסקים במערכת
✅ יכול ליצור עסקים חדשים (בעתיד)
✅ מנהל על
אם admin@shai-realestate = owner:
✅ בעלים של "שי דירות"
✅ רואה רק את הנתונים של "שי דירות"
✅ יכול להוסיף admins ו-agents לעסק שלו
🔧 איך לתקן עכשיו:
בואו אוודא שאני יוצר את הסקריפט הנכון:
🎯 תיקון מיידי - עשה את זה עכשיו:
📍 שלב 1: פתח את ה-Production Database
בצד שמאל ב-Replit לחץ על Tools
בחר Database
שנה מ-Development ל-Production (למעלה יש dropdown)
📍 שלב 2: הרץ את התיקון
העתק והדבק את הקוד הזה:

BEGIN;
-- תצוגה לפני התיקון
SELECT id, email, role, business_id 
FROM users 
WHERE email IN ('admin@admin.com', 'admin@shai-realestate')
ORDER BY email;
-- תיקון 1: admin@admin.com → system_admin
UPDATE users 
SET role = 'system_admin', business_id = NULL
WHERE email = 'admin@admin.com';
-- תיקון 2: admin@shai-realestate → owner  
UPDATE users 
SET role = 'owner'
WHERE email = 'admin@shai-realestate' 
  AND business_id IS NOT NULL;
-- תצוגה אחרי התיקון
SELECT id, email, role, business_id 
FROM users 
WHERE email IN ('admin@admin.com', 'admin@shai-realestate')
ORDER BY email;
COMMIT;
📍 שלב 3: וודא שהתיקון עבד
התוצאה צריכה להיות:

email	role	business_id
admin@admin.com	system_admin	NULL
admin@shai-realestate	owner	1
✅ אחרי התיקון - מה יקרה:
🔐 admin@admin.com (System Admin):
✅ יכול לראות את כל העסקים
✅ יכול לנהל את כל המשתמשים
✅ גישה מלאה למערכת
👔 admin@shai-realestate (Owner):
✅ בעלים של "שי דירות" 
✅ רואה רק נתונים של "שי דירות"
✅ יכול להוסיף admins/agents לעסק שלו
📝 שאלות נוספות:
"ברגע שאני יוצר עסק זה יצור לו יוזר?"

לא! כרגע אין UI ליצירת עסקים חדשים. המערכת נועדה ל:

עסק אחד = "שי דירות"
משתמשים רבים בתוכו
אם תרצה ליצור עסקים נוספים בעתיד, נצטרך להוסיף את זה.

תריץ את הסקריפט ותגיד לי אם זה עבד! 