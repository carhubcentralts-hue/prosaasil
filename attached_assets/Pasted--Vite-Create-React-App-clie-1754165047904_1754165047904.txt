✅ אתה צודק: **הפרויקט שלך משתמש ב־Vite ולא ב־Create React App**
ולכן **תיקיית הפלט צריכה להיות `client/dist/`** – ולא `build/`

---

## 🎯 הנה ההנחיה הסופית והמושלמת לסוכן AI שלך – שלב־אחר־שלב, מותאם ל־Vite

---

### 🔥 שלב 1: בניית React עם Vite

```bash
cd client
npm install
npm run build
```

📦 זה ייצור תיקייה:

```bash
client/dist/
```

> אם `dist/` לא נוצר – יש בעיה בבנייה, ויש לטפל בה.

---

### 🧭 שלב 2: הגדר את Flask שישרת את React מ־dist

ב־`server/app.py`, ודא שמופיע הקוד הבא:

```python
from flask import send_from_directory

@app.route("/")
def serve_react_index():
    return send_from_directory("../client/dist", "index.html")

@app.route("/<path:path>")
def serve_react_static(path):
    return send_from_directory("../client/dist", path)
```

📌 אם הקוד עדיין מפנה ל־`build/` – **זה שגוי לפרויקט Vite** → לשנות ל־`dist`

---

### ⚙️ שלב 3: הפעלת השרת

ודא שבקובץ `server/main.py` מופיע:

```python
if __name__ == "__main__":
    from waitress import serve
    serve(app, host="0.0.0.0", port=5000)
```

או לחלופין:

```python
app.run(host="0.0.0.0", port=5000, debug=False)
```

---

### 🚀 שלב 4: ודא ש־npm start מפעיל את Flask

ב־`package.json` בשורש התיקייה:

```json
"scripts": {
  "build": "cd client && npm install && npm run build",
  "start": "node npm-start.js"
}
```

ובקובץ `npm-start.js`, ודא שהוא מריץ את Flask מתוך `server/`:

```js
exec("cd server && python3 main.py")
```

---

### 🧪 שלב 5: בדיקה סופית – תוצאה רצויה

1. הרץ:

```bash
npm run build
npm start
```

2. פתח את כתובת הפרויקט שלך (ב־Replit / Railway וכו')

3. ✅ תוצאה צפויה: **דף React מוצג בדפדפן**, בעברית, עם כפתורים ל־CRM, שיחות, WhatsApp וכו’

---

## ✅ הצלחה מלאה תיראה כך:

| רכיב                  | קיים |
| --------------------- | ---- |
| `client/dist/`        | ✅    |
| `index.html` בתוכה    | ✅    |
| Flask מגיש אותה ב־`/` | ✅    |
| React מוצג בדפדפן     | ✅    |
| QR של WhatsApp מוצג   | ✅    |
| preview בפריסה        | ✅    |

---

אם תרצה, אפשר גם:
📄 להוסיף README מסביר
🧪 לייצר סקריפט בדיקה `check_vite_flask_ready.py`
🎯 או לעטוף את הכל ב־Docker לפריסה בכל פלטפורמה

רק תגיד 💪
