פתחתי את ה-ZIP האחרון ובדקתי בפועל. הנה מצב אמת + הנחיות תיקון מדויקות להביא ל-100% לפני המעבר לויזואל:

פסק דין מהיר (GO/NO-GO)
	•	🎙️ טלפוניה “שיחה חיה”: NO-GO. כרגע /webhook/incoming_call מחזיר <Play> … <Record> (תיבת הודעות), אין Media Streams ואין WebSocket. ראיות: AgentLocator/server/routes_twilio.py — הפונקציה incoming_call() מחזירה <Play>+<Record>; אין שום קובץ/קוד שמכיל "/ws/twilio-media" או flask_sock.
	•	🗣️ ברכת פתיחה אוטומטית לעסק: קיימת ברירת מחדל וקבצי MP3 קיימים (server/static/voice_responses/welcome.mp3, ועוד). יש גם סקריפט יצירה create_hebrew_greeting.py. אבל הזרימה לא מייצרת ברכה פר-עסק בזמן אמת; כרגע תמיד חוזר welcome.mp3.
	•	💬 WhatsApp: יש ספק מאוחד (Twilio + Baileys) וקבצי worker (baileys_client.js) ותור. אבל ה-API הפעיל הוא server/whatsapp_api.py (מוק), בעוד ה-API המשופר server/api_whatsapp_improved.py לא רשום באפליקציה.
	•	🔐 אבטחה: אימות חתימת Twilio קיים (server/twilio_verify.py) ומופעל על ה-webhooks — טוב.
	•	🧩 Google TTS: ה-bootstrap מחפש GOOGLE_TTS_SA_JSON בלבד. אם אתה מזין GOOGLE_CLOUD_SERVICE_ACCOUNT_JSON זה לא ייטען.
	•	💳 סליקה/חשבוניות/חוזים (CRM): לא ממומש. אין מודלים/ראוטים ל-Payment/Invoice/Contract (ראיתי את server/models_sql.py – אין מחלקות כאלה).

⸻

מה לתקן – קופי/פייסט לפי קובץ

(מינימום שינוי להריץ “שיחה בעברית בזמן אמת” + WhatsApp מאוחד + CRM עם סליקה/חשבוניות/חוזים. איפה שצריך – מוסיפים קבצים חדשים ייעודיים.)

1) שיחות חיות: TwiML → Media Streams + WS

1.1 server/routes_twilio.py — להחליף את TwiML

כרגע יש <Play> + <Record> והודעת <Say language="he-IL"> (שגם תגרור אזהרת 13512 כי Twilio לא תומך he-IL). החלף ל-Stream בלבד:

# server/routes_twilio.py
from flask import Blueprint, request, Response
from server.twilio_verify import require_twilio_signature
import os, logging
twilio_bp = Blueprint("twilio_bp", __name__, url_prefix="")
log = logging.getLogger("twilio.unified")

def _xml(x): return Response(x, status=200, mimetype="text/xml")

@twilio_bp.post("/webhook/incoming_call")
@require_twilio_signature
def incoming_call():
    host = (os.getenv("PUBLIC_HOST") or "").rstrip("/")
    assert host, "PUBLIC_HOST must be set"
    xml = f"""<?xml version="1.0" encoding="UTF-8"?>
<Response>
  <Connect>
    <Stream url="wss://{host.replace('https://','').replace('http://','')}/ws/twilio-media"/>
  </Connect>
</Response>"""
    return _xml(xml)

זה מסיר לגמרי <Say>/<Record> ולכן פותח זרימת קול דו-כיוונית בזמן אמת.

1.2 server/app_factory.py — להוסיף WS בזמן ריצה

נוסיף WebSocket עם flask_sock, בלי ליצור קובץ נוסף:

# בתוך create_app(), אחרי יצירת app
from flask_sock import Sock
sock = Sock(app)

import json, base64, time, audioop, tempfile, wave, os
from google.cloud import texttospeech

@sock.route("/ws/twilio-media")
def ws_twilio_media(ws):
    stream_sid = None
    buf_lin16_8k = b""

    def tts_and_stream_he(text: str):
        if not text: return
        try:
            client = texttospeech.TextToSpeechClient()
            voice = texttospeech.VoiceSelectionParams(language_code="he-IL",
                                                      name=os.getenv("TTS_VOICE","he-IL-Wavenet-A"))
            cfg = texttospeech.AudioConfig(
                audio_encoding=texttospeech.AudioEncoding.LINEAR16,
                sample_rate_hertz=8000
            )
            resp = client.synthesize_speech(texttospeech.SynthesisInput(text=text), voice, cfg)
            lin16_8k = resp.audio_content  # bytes PCM16@8k
            mulaw = audioop.lin2ulaw(lin16_8k, 2)
            for i in range(0, len(mulaw), 160):  # 20ms @8k
                payload = base64.b64encode(mulaw[i:i+160]).decode()
                ws.send(json.dumps({"event":"media","streamSid": stream_sid, "media":{"payload": payload}}))
                time.sleep(0.02)
        except Exception as e:
            current_app.logger.error(f"TTS error: {e}")

    def flush_and_transcribe():
        nonlocal buf_lin16_8k
        if not buf_lin16_8k: return ""
        try:
            # 8k→16k Whisper
            converted, _ = audioop.ratecv(buf_lin16_8k, 2, 1, 8000, 16000, None)
            fd, wav_path = tempfile.mkstemp(suffix=".wav"); os.close(fd)
            with wave.open(wav_path, "wb") as wf:
                wf.setnchannels(1); wf.setsampwidth(2); wf.setframerate(16000)
                wf.writeframes(converted)
            # Whisper API (התאם אם אתה משתמש במודל אחר)
            import openai, os as _os
            client = openai.OpenAI(api_key=_os.getenv("OPENAI_API_KEY"))
            with open(wav_path, "rb") as f:
                tr = client.audio.transcriptions.create(model="whisper-1", file=f, language="he")
            text = (tr.text or "").strip()
            os.remove(wav_path)
            buf_lin16_8k = b""
            return text
        except Exception as e:
            current_app.logger.error(f"ASR error: {e}")
            buf_lin16_8k = b""
            return ""

    while True:
        msg = ws.receive()
        if msg is None: break
        data = json.loads(msg); ev = data.get("event")
        if ev == "start":
            stream_sid = data["start"]["streamSid"]
        elif ev == "media":
            mulaw = base64.b64decode(data["media"]["payload"])
            lin16_8k = audioop.ulaw2lin(mulaw, 2)
            buf_lin16_8k += lin16_8k
            # flush “אמירה” אחרי ~1.2 שניות קול
            if len(buf_lin16_8k) > int(1.2*8000*2):
                user_text = flush_and_transcribe()
                if user_text:
                    # כאן לוגיקת GPT (החלף במודל/פרומפט שלך)
                    reply = user_text
                    tts_and_stream_he(reply)
        elif ev == "stop":
            user_text = flush_and_transcribe()
            if user_text:
                tts_and_stream_he("תודה, נתראה!")
            break

זה מספיק כדי להוכיח דו-כיווני חי. אחר-כך אפשר להשלים לוגיקת GPT מתקדמת, VAD, זיכרון וכו’.

⸻

2) Google TTS — לקלוט גם GOOGLE_CLOUD_SERVICE_ACCOUNT_JSON

עדכן server/bootstrap_secrets.py:

# בתחתית ensure_google_creds_file() – במקום לבדוק רק GOOGLE_TTS_SA_JSON
raw = os.getenv("GOOGLE_TTS_SA_JSON") or os.getenv("GOOGLE_CLOUD_SERVICE_ACCOUNT_JSON")
if not raw:
    print("⚠️ No Google SA JSON found"); return
try:
    import json, base64, tempfile
    try:
        obj = json.loads(raw)
    except Exception:
        obj = json.loads(base64.b64decode(raw).decode("utf-8"))
    tmp = tempfile.NamedTemporaryFile("w", suffix=".json", delete=False)
    json.dump(obj, tmp); tmp.flush(); tmp.close()
    os.environ["GOOGLE_APPLICATION_CREDENTIALS"] = tmp.name
    print(f"✅ Google credentials file created: {tmp.name}")
except Exception as e:
    raise RuntimeError(f"Invalid Google SA JSON: {e}")

וב-main.py כבר יש:

from server.bootstrap_secrets import ensure_env, ensure_google_creds_file
ensure_env(); ensure_google_creds_file()


⸻

3) WhatsApp — לרשום את ה-API המאוחד ולחבר לספקים

ב-server/app_factory.py רשום גם את ה-BP המשופר:

from server.api_whatsapp_improved import whatsapp_bp
app.register_blueprint(whatsapp_bp)

דאג שה-env מכוון:

WHATSAPP_PROVIDER=baileys   # או twilio
TWILIO_WA_FROM=whatsapp:+14155238886

וה-worker:
	•	הפעל baileys_client.js (PM2/Procfile) ושמור את baileys_auth_info/ על דיסק קבוע.
	•	בדיקה: GET /api/whatsapp/status צריך להחזיר connected:true או QR.

⸻

4) CRM – להוסיף סליקה/חשבוניות/חוזים

4.1 מודלים — הוסף ל-server/models_sql.py:

from server.db import db
from datetime import datetime

class Payment(db.Model):
    __tablename__ = "payment"
    id = db.Column(db.Integer, primary_key=True)
    deal_id = db.Column(db.Integer, db.ForeignKey("deal.id"), nullable=False)
    stripe_payment_intent = db.Column(db.String(80), unique=True)
    amount = db.Column(db.Integer, nullable=False)  # אגורות
    currency = db.Column(db.String(8), default="ils")
    status = db.Column(db.String(32), default="created")  # created|succeeded|failed
    created_at = db.Column(db.DateTime, default=datetime.utcnow)

class Invoice(db.Model):
    __tablename__ = "invoice"
    id = db.Column(db.Integer, primary_key=True)
    deal_id = db.Column(db.Integer, db.ForeignKey("deal.id"), nullable=False)
    invoice_number = db.Column(db.String(40), unique=True, index=True)
    total = db.Column(db.Integer)
    pdf_path = db.Column(db.String(260))
    issued_at = db.Column(db.DateTime, default=datetime.utcnow)

class Contract(db.Model):
    __tablename__ = "contract"
    id = db.Column(db.Integer, primary_key=True)
    deal_id = db.Column(db.Integer, db.ForeignKey("deal.id"), nullable=False)
    template_name = db.Column(db.String(80))
    pdf_path = db.Column(db.String(260))
    signed_name = db.Column(db.String(160))
    signed_at = db.Column(db.DateTime)
    signed_ip = db.Column(db.String(64))

(הרץ מיגרציה / db.create_all() כפי שאתה עובד היום.)

4.2 סליקה (Stripe) — הוסף ל-API (למשל server/api_crm_unified.py)

import stripe, os
from server.models_sql import Payment, Deal
from server.db import db
stripe.api_key = os.getenv("STRIPE_SECRET_KEY")

@crm_unified_bp.post("/payments/create-intent")
def create_payment_intent():
    data = request.get_json() or {}
    deal_id = int(data["deal_id"]); amount = int(data["amount"])
    currency = (os.getenv("CURRENCY") or "ils").lower()
    pi = stripe.PaymentIntent.create(amount=amount, currency=currency, automatic_payment_methods={"enabled": True})
    db.session.add(Payment(deal_id=deal_id, stripe_payment_intent=pi["id"], amount=amount, currency=currency))
    db.session.commit()
    return jsonify({"client_secret": pi["client_secret"], "payment_intent": pi["id"]}), 200

@crm_unified_bp.post("/webhook/stripe")
def stripe_webhook():
    sig = request.headers.get("Stripe-Signature","")
    secret = os.getenv("STRIPE_WEBHOOK_SECRET")
    try:
        event = stripe.Webhook.construct_event(request.data, sig, secret)
    except Exception as e:
        return (str(e), 400)
    if event["type"] == "payment_intent.succeeded":
        pi = event["data"]["object"]
        pay = Payment.query.filter_by(stripe_payment_intent=pi["id"]).first()
        if pay: pay.status="succeeded"; db.session.commit()
        # הפקת חשבונית אוטומטית (סעיף הבא)
        try: create_invoice_for_payment(pay)
        except Exception as e: current_app.logger.error(f"Invoice gen failed: {e}")
    return ("", 204)

4.3 חשבוניות — הוסף שירות פשוט (קובץ חדש, הכרחי): server/services/invoice_service.py

from server.models_sql import Invoice, Deal, Payment
from server.db import db
from jinja2 import Template
from datetime import datetime
import os, pdfkit

def next_invoice_number():
    prefix = os.getenv("INVOICE_PREFIX","INV")
    seq = (Invoice.query.count() + 1)
    return f"{prefix}-{datetime.utcnow():%Y%m}-{seq:04d}"

def create_invoice_for_payment(payment: Payment):
    deal = Deal.query.get(payment.deal_id)
    inv_no = next_invoice_number()
    html = Template("""<html dir="rtl"><meta charset="utf-8"/><body>
    <h2>חשבונית {{inv}}</h2><p>לקוח: {{deal.customer_name}}</p>
    <p>סכום: {{amt}} ₪</p><p>תאריך: {{now}}</p></body></html>""").render(
        inv=inv_no, deal=deal, amt=payment.amount/100, now=datetime.utcnow())
    out_dir = os.path.join("server","static","invoices"); os.makedirs(out_dir, exist_ok=True)
    pdf_path = os.path.join(out_dir, f"{inv_no}.pdf")
    try: pdfkit.from_string(html, pdf_path)
    except Exception: open(pdf_path.replace(".pdf",".html"),"w",encoding="utf-8").write(html)
    db.session.add(Invoice(deal_id=deal.id, invoice_number=inv_no, total=payment.amount, pdf_path=pdf_path)); db.session.commit()
    return inv_no

והורדה:

@crm_unified_bp.get("/invoices/<invoice_number>")
def get_invoice(invoice_number):
    inv = Invoice.query.filter_by(invoice_number=invoice_number).first_or_404()
    return send_file(inv.pdf_path, as_attachment=True)

4.4 חוזים — תצוגה וחתימה

הוסף ל-API:

@crm_unified_bp.get("/contracts/preview")
def contract_preview():
    deal_id = int(request.args["deal_id"])
    html = "<html dir='rtl'><meta charset='utf-8'/><body><h2>חוזה לדיל #{}</h2></body></html>".format(deal_id)
    return Response(html, 200, mimetype="text/html")

@crm_unified_bp.post("/contracts/sign")
def contract_sign():
    data = request.get_json() or {}
    deal_id = int(data["deal_id"]); signer = data.get("name","")
    sig_b64 = (data.get("signature_b64","").split(",")[-1] if data.get("signature_b64") else "")
    # רנדר HTML → PDF (בדומה לחשבונית), שמור Contract עם signed_name/signed_at/signed_ip
    ...

(תוכל לשדרג בהמשך ל-DocuSign/Dropbox Sign; כרגע זה מספיק ל-MVP).

⸻

5) ברכה לעסק — ייצור אוטומטי אם חסרה

ב-server/routes_twilio.py החלף את get_business_greeting() כך:

def get_business_greeting(to_number, call_sid):
    # TODO: למפות business לפי מס' יעד/דומיין
    path = "static/voice_responses/welcome.mp3"
    abs_path = os.path.join(os.path.dirname(__file__), "static", "voice_responses", "welcome.mp3")
    if not os.path.exists(abs_path):
        try:
            from server.hebrew_tts_enhanced import create_hebrew_audio
            text = "שלום וברוכים הבאים. כיצד אפשר לעזור?"
            rel = create_hebrew_audio(text, call_sid or "greeting") or path
            return rel
        except Exception:
            return path
    return path


⸻

6) ניקוי כפילויות סטטיות

יש גם קבצים תחת server/server/static/voice_responses/* (תיקיה כפולה). השאר שימוש רק ב-server/static/voice_responses/. אם נתיבים כפולים נטענים — מחק רישום/שימוש בנתיב הישן (לא חייב למחוק קבצים מה־repo, רק לא להשתמש בהם).

⸻

7) בדיקות GO/NO-GO (להריץ)

# Health
curl -s -o /dev/null -w "HTTP %{http_code}\n" $PUBLIC_HOST/api/health

# Voice TwiML -> חייב להכיל Stream
curl -s -X POST $PUBLIC_HOST/webhook/incoming_call | grep -q "<Connect><Stream" && echo "Voice Stream OK"

# WS — לבצע שיחה אמיתית ולראות start/media/stop בלוגים

# Google TTS עולה
python - <<'PY'
import os
from google.cloud import texttospeech
print("GAC set?", bool(os.getenv("GOOGLE_APPLICATION_CREDENTIALS")))
texttospeech.TextToSpeechClient(); print("TTS OK")
PY

# WhatsApp (BAILEYS)
curl -s $PUBLIC_HOST/api/whatsapp/status

# Stripe
curl -s -X POST $PUBLIC_HOST/api/payments/create-intent -H "Content-Type: application/json" -d '{"deal_id":1,"amount":9900}'


⸻

מה עובד כבר עכשיו ומה לא
	•	✅ קבצי קול וברכה קיימים — טוב.
	•	✅ אימות Twilio — טוב.
	•	⚠️ שיחה חיה — לא (אין Media Streams/WS).
	•	⚠️ WhatsApp — יש תשתיות, אבל ה-API המשופר לא רשום; ה-worker צריך לרוץ.
	•	❌ סליקה/חשבוניות/חוזים — חסר; הוספתי מימוש מינימלי ברור.

⸻

סיכום חד

אם תעשה את 1–5 למעלה (בעיקר 1.1+1.2 ו-2), אתה עובר ל-GO לשיחות חיות דו-כיווניות בעברית עם תמלול בזמן אמת. רישום ה-API של WhatsApp המאוחד + הפעלת ה-Baileys יתנו MAU קבוע. הוספת הסליקה/חשבוניות/חוזים תשלים את ה-CRM ל-100% לקראת שכבת UI/ויזואל.

כשתסיים ונריץ smoke — נעבור למסכים (דשבורד, תשלום, חתימה) בביטחון.