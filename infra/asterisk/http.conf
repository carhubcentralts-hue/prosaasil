;
; Asterisk HTTP Server Configuration
; Required for ARI WebSocket/REST API
;

[general]
enabled = yes
bindaddr = 0.0.0.0
bindport = 8088

; Enable WebSockets for ARI events
enablestatic = yes

; CORS for ARI (adjust in production)
enable_cors = yes
