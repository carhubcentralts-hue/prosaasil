BUILD 77 - Automatic Production Database Initialization

🚀 תוספות BUILD 77:
1. ✅ אתחול אוטומטי למסד נתונים בפרודקשן
   - מערכת initialize_production_database() רצה אוטומטית בהפעלה
   - יצירה אוטומטית של עסק ראשי אם לא קיים
   - יצירה אוטומטית של משתמש admin@admin.com
   - קישור אוטומטי של admin לעסק
   - יצירה אוטומטית של 7 סטטוסים בסיסיים עבריים
2. ✅ פתרון מסחרי מקצועי - עובד out-of-the-box
   - כל deployment חדש מאותחל אוטומטית
   - אין צורך בהגדרות ידניות
   - מתאים לפריסה מסחרית מיידית
3. ✅ Idempotent - בטוח להריץ מספר פעמים
   - בודק אם כבר קיימים נתונים לפני יצירה
   - לוגים ברורים של כל שלב
   - לא קורס אם יש שגיאה

🔧 תוספות BUILD 76:
1. ✅ Admin יכול לנהל סטטוסים (ללא business_id)
   - GET /api/statuses - גישה לכל הסטטוסים
   - POST /api/statuses - יצירת סטטוסים לכל עסק
   - PUT /api/statuses/:id - עדכון כל סטטוס
   - DELETE /api/statuses/:id - מחיקת כל סטטוס
2. ✅ מחיקה קבוצתית של לידים
   - Endpoint חדש: POST /api/leads/bulk-delete
   - Admin יכול למחוק לידים בכל הטננטים
   - בקרת גישה ורישום פעילות
3. ✅ בחירת לידים עם checkboxes
   - Checkbox לכל ליד
   - Checkbox לבחירת הכל
   - כפתור מחיקה קבוצתית
   - אישור מחיקה

פרטי כניסה:
📧 admin@admin.com
🔑 admin123
