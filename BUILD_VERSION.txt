62
## Recent Changes (2025-10-08)

### Build 62 - CRITICAL FIX: Error 12100 סופית נפתר!
- **🔧 Twilio SDK (VoiceResponse)** - החלפת XML ידני ב-Twilio SDK הרשמי
- **✅ `_twiml()` helper** - פונקציה אחידה להחזרת TwiML תקין
- **📝 Content-Type תקין** - `text/xml; charset=utf-8` בכל response
- **🎯 /webhook/incoming_call** - שימוש ב-VoiceResponse().connect().stream()
- **🎯 /webhook/incoming_call_preview** - גם כן עם SDK
- **🚀 Build validation** - TwiML מאומת שמתחיל ב-<?xml ומכיל <Response><Connect><Stream>

### Recording Retention System (Build 61)
- Changed recording retention from 7 days to **2 days (48 hours exactly)**
- Added `auto_cleanup_old_recordings()` function for automated cleanup
- Updated `POST /api/calls/cleanup` to delete both DB entries and disk files
- Fixed expiry check to be exactly 48 hours (not 71h59m)
- Transcriptions are preserved indefinitely
