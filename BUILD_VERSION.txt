BUILD 78 - Production Database Auto-Initialization (COMPLETE!)

🔧 תיקונים קריטיים BUILD 78:
1. ✅ תוקן שדה 'active' שלא קיים
   - **BEFORE**: Business(active=True) → 'active' is invalid ❌
   - **AFTER**: Business(is_active=True) → Works! ✅
2. ✅ תוקן שדה phone_number חובה
   - **BEFORE**: Business() ללא phone_number → NOT NULL error ❌
   - **AFTER**: Business(phone_e164="+972500000000") → Works! ✅
3. ✅ תוקן שדה greeting_message חובה
   - **BEFORE**: Business() ללא greeting_message → NOT NULL error ❌
   - **AFTER**: Business(greeting_message="שלום! איך אפשר לעזור?") → Works! ✅
4. ✅ לוגים מפורשים לדיבוג
   - הוספתי print() לכל שלב בנוסף ל-logger
   - traceback מלא אם יש שגיאה
5. ✅ אתחול אוטומטי למסד נתונים בפרודקשן
   - מערכת initialize_production_database() רצה אחרי migrations
   - יצירה אוטומטית של עסק "עסק ראשי" אם לא קיים
   - יצירה אוטומטית של משתמש admin@admin.com עם business_id
   - יצירה אוטומטית של 7 סטטוסים בסיסיים עבריים
6. ✅ Idempotent - בטוח להריץ מספר פעמים
   - בודק אם כבר קיימים נתונים לפני יצירה
   - לוגים ברורים של כל שלב
7. ✅ מוכן לפריסה מסחרית
   - בפריסה: RUN_MIGRATIONS_ON_START=1 (מוגדר ב-start_production.sh)
   - כל deployment חדש מאותחל אוטומטית
   - עובד out-of-the-box ללא הגדרות ידניות

🔧 תוספות BUILD 76:
1. ✅ Admin יכול לנהל סטטוסים (ללא business_id)
   - GET /api/statuses - גישה לכל הסטטוסים
   - POST /api/statuses - יצירת סטטוסים לכל עסק
   - PUT /api/statuses/:id - עדכון כל סטטוס
   - DELETE /api/statuses/:id - מחיקת כל סטטוס
2. ✅ מחיקה קבוצתית של לידים
   - Endpoint חדש: POST /api/leads/bulk-delete
   - Admin יכול למחוק לידים בכל הטננטים
   - בקרת גישה ורישום פעילות
3. ✅ בחירת לידים עם checkboxes
   - Checkbox לכל ליד
   - Checkbox לבחירת הכל
   - כפתור מחיקה קבוצתית
   - אישור מחיקה

פרטי כניסה:
📧 admin@admin.com
🔑 admin123
