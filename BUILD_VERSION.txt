BUILD 79 - Business & Lead Creation Fix + Full CRUD Support

🔧 תיקונים קריטיים BUILD 79:
1. ✅ תוקנה יצירת עסק - הוספת כל השדות הנדרשים
   - whatsapp_number, greeting_message, whatsapp_greeting
   - system_prompt, voice_message, working_hours
   - phone_permissions, whatsapp_permissions
   - calls_enabled, crm_enabled, whatsapp_enabled
   - payments_enabled, default_provider
2. ✅ תוקן עדכון עסק - תמיכה מלאה בכל השדות
   - עדכון כל השדות החדשים
   - auto-update של updated_at
3. ✅ תגובות CRUD קונסיסטנטיות
   - GET/POST/PUT מחזירים את כל השדות המאוחסנים
   - is_active, timestamps, boolean flags
   - status נקבע לפי is_active (active/inactive)
4. ✅ יצירת ליד עובדת תקין - מחוברת למסד נתונים
5. ✅ מוכן לשימוש מלא - CRUD מלא לעסקים ולידים

BUILD 78 - Production Database Auto-Initialization (COMPLETE!)

🔧 תיקונים קריטיים BUILD 78 - Schema Drift Fix:
1. ✅ תוקן שדה 'active' שלא קיים → is_active
2. ✅ תוקן שדה phone_number חובה → phone_e164="+972500000000"
3. ✅ תוקן שדה greeting_message חובה → "שלום! איך אפשר לעזור?"
4. ✅ תוקן שדה system_prompt חובה → "את לאה, סוכנת נדל\"ן מקצועית..."
5. ✅ הוספת כל שדות ה-Business הנדרשים:
   - whatsapp_number, whatsapp_greeting, voice_message
   - phone_permissions, whatsapp_permissions
   - payments_enabled, default_provider, working_hours
6. ✅ לוגים מפורשים עם print() + logger
7. ✅ אתחול אוטומטי למסד נתונים בפרודקשן
   - initialize_production_database() רץ אחרי migrations
   - יצירת עסק "עסק ראשי" עם כל השדות הנדרשים
   - יצירת admin@admin.com עם business_id
   - יצירת 7 סטטוסים עבריים
8. ✅ Idempotent - בטוח להריץ מספר פעמים
9. ✅ מוכן לפריסה מסחרית - עובד out-of-the-box

🔧 תוספות BUILD 76:
1. ✅ Admin יכול לנהל סטטוסים (ללא business_id)
   - GET /api/statuses - גישה לכל הסטטוסים
   - POST /api/statuses - יצירת סטטוסים לכל עסק
   - PUT /api/statuses/:id - עדכון כל סטטוס
   - DELETE /api/statuses/:id - מחיקת כל סטטוס
2. ✅ מחיקה קבוצתית של לידים
   - Endpoint חדש: POST /api/leads/bulk-delete
   - Admin יכול למחוק לידים בכל הטננטים
   - בקרת גישה ורישום פעילות
3. ✅ בחירת לידים עם checkboxes
   - Checkbox לכל ליד
   - Checkbox לבחירת הכל
   - כפתור מחיקה קבוצתית
   - אישור מחיקה

פרטי כניסה:
📧 admin@admin.com
🔑 admin123
